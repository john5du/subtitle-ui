/** Shared control density class tokens. Prefer Button/Input/Select size props when available. */

/** Subtitle row / track actions: touch-friendly on mobile, compact on sm+. */
export const rowActionIconClassName = "h-10 w-10 shrink-0 touch-target sm:h-8 sm:w-8";
export const rowActionTextClassName = "h-10 shrink-0 gap-1 px-2.5 text-caption touch-target sm:h-8";

/** Settings row min height. */
export const settingsRowMinClassName = "min-h-[56px]";
