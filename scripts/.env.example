# Local development environment for subtitle-ui.
# Copy to scripts/.env and fill in values (never commit scripts/.env):
#   cp scripts/.env.example scripts/.env
#
# Loaded automatically by ./scripts/dev-up.sh and ./scripts/dev-restart.sh.
# Shell-exported variables take precedence over this file.

# --- Auth ---
# Required: set a strong secret. Default "change-me" is rejected unless the next line is true.
# ADMIN_TOKEN=your-strong-secret
# ALLOW_INSECURE_DEFAULT_ADMIN_TOKEN=true

# --- MCP (AI agents; Streamable HTTP at /mcp, same Bearer token; default off) ---
# MCP_ENABLED=false
# Or enable at runtime: Settings → MCP, or PUT /api/config/mcp {"enabled":true}

# --- Media roots (defaults: ./media/movies, ./media/tv) ---
# MOVIE_MEDIA_ROOT=./media/movies
# TV_MEDIA_ROOT=./media/tv
# MEDIA_ROOT is deprecated; do not set it for new installs.

# --- Database (PostgreSQL required) ---
# If unset, ./scripts/dev-up.sh starts the dev-only Compose Postgres
# (127.0.0.1:5432, postgres:16, user/password/db: subtitle/subtitle/subtitle_ui)
# and waits until it is healthy.
# DATABASE_URL=postgres://subtitle:subtitle@127.0.0.1:5432/subtitle_ui?sslmode=disable
# TEST_POSTGRES_DSN=postgres://subtitle:subtitle@127.0.0.1:5432/subtitle_ui?sslmode=disable

# --- CORS (dev-up defaults to localhost:3300 if unset) ---
# CORS_ALLOWED_ORIGINS=http://localhost:3300,http://127.0.0.1:3300

# --- SubHD (optional) ---
# SUBHD_ENABLED=true
# SUBHD_BASE_URL=https://subhd.tv
# SUBHD_PROXY=socks5://127.0.0.1:1080

# --- Sonarr (optional; enables TV season completeness + missing search) ---
# SONARR_URL=http://127.0.0.1:8989
# SONARR_API_KEY=your-sonarr-api-key
# SONARR_ENABLED=true

# --- Jellyfin (optional; subtitle notify + video play-preview stream proxy) ---
# JELLYFIN_URL=http://127.0.0.1:8096
# JELLYFIN_API_KEY=your-jellyfin-api-key
# JELLYFIN_ENABLED=true
# JELLYFIN_PATH_MAP=/host/movies:/data/movies,/host/tv:/data/tv
# JELLYFIN_USER_ID=  # optional GUID for stream PlaybackInfo; empty auto-picks an admin user

# --- Video stream preview tickets (requires Jellyfin; proxies static stream) ---
# STREAM_TICKET_SECRET=  # required in production; empty in dev → derive from ADMIN_TOKEN
# STREAM_TICKET_TTL=15m
# MCP_CONFIRM_SECRET=    # optional; empty → STREAM_TICKET_SECRET, then ADMIN_TOKEN

# --- Frontend (optional; only used by Next if set when FE starts) ---
# NEXT_PUBLIC_API_BASE=http://localhost:9307
